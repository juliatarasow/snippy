version: '3.8'

services:
  server:
    build: ./server
    ports:
      - "4000:4000"
    env_file:
      - ./server/.env
    restart: unless-stopped

  client:
    build: ./client-master
    ports:
      - "80:80"
    depends_on:
      - server
    restart: unless-stopped